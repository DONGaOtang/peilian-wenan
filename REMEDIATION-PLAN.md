# 补救与补齐方案（可实施补丁）

> 配套文件：REVIEW-REPORT.md（审查报告）。本文件是可直接粘贴进 skill 的修复文本。
# 补救与补齐方案（可实施补丁）

> 配套文件：REVIEW-REPORT.md（审查报告）。本文件是可直接粘贴进 skill 的修复文本。
> 标注：🔴P0 立即修复 ｜ 🟠P1 短期补齐 ｜ 🟡P2 中期增强

> **2026-08-18 状态：P0/P1/P2 全部修复已实际应用到 skill 文件**（执行明细见 REVIEW-REPORT.md 末尾「修复执行记录」）。
> 本文件的补丁文本仍保留，供日后回归与复现。

---

## 🔴 P0-1 平台声明（SKILL.md 开头「一句话定位」之后插入）

```markdown
**平台口径**：本系统的数据指标（2s 跳出率/完播率/5s 数据/完播率-时长曲线）为**抖音创作者后台**口径。
小红书（收藏率/互动率）、视频号（分享率/完播口径）、B 站（完播率口径不同）需按附录《平台指标替换表》换算后再用本系统判据。
```

## 🔴 P0-2 统一六官→七官（six-judges.md 末尾）

把「dbs-script-flow 可在六官之后追加运行」改为：

```markdown
- dbs-script-flow 可在七官之后追加运行（其逻辑延续检查与逻辑官/口语官互补，尤其补「生造概念/术语未解释」检查项）
- dbs-ai-check 可在 IP官 环节辅助检测 AI 味——注意其立场是「只识别不改、反对伪装成人类」：
  本系统的用法是借它的 22 个特征做**初筛提示**，最终改稿仍以「说话指纹对齐」为准，不模板化注入停顿/改口
```

## 🔴 P0-3 节奏官/结构官职责切分（six-judges.md）

**节奏官**改为：只管**句级留存的连续性**——每一句是否让观众想听下一句、长句/绕句/低密度填充词的删法、2s 钩子句是否成立（微观）。
**结构官**改为：只管**骨架宏观节拍是否跑偏**——对照 P4 分段骨架逐段核对（该段功能是否兑现、秒数是否匹配、结尾履约点是否送达），**不再重复 2s/5s/15-20s 的逐点复查**（那是节奏官/逻辑官的活）。

## 🔴 P0-4 契约问法接口统一（contracts.md + dialogue-guide.md）

方案：contracts.md 的「P3 挖素材问法」全部改名并降级为 dialogue-guide 第二轮（经历轮）的「按契约追问变体」。dialogue-guide.md 第二轮改为：

```markdown
### 第二轮：亲身经历（高光挖掘轮）

标准问法：「这件事你有亲身经历吗？」

**按契约追问变体**（替代 contracts.md 里的「P3 挖素材问法」字段，统一以本表为准）：
| 契约 | 经历轮追问 |
|------|-----------|
| 确定感约 | 「你踩过这个坑吗？最后怎么爬出来的？」 |
| 共鸣约 | 「那件事最具体的细节是什么？当时心里第一反应是什么？」 |
| 爽感约 | 「你经历过最气的场面是什么？当时想说什么但没说出口？」 |
| 希望约 | 「你从什么状态爬起来的？最低点是什么样的？」 |
| 优越约 | 「这行里有什么外行根本不知道的事？」 |
| 新奇约 | 「你知道这事后，看世界的眼光变了吗？哪里变了？」 |
```

contracts.md 各契约的「P3 挖素材问法」字段删除或加注「→ 见 dialogue-guide.md 第二轮按契约追问变体」。

## 🔴 P0-5 标题公式补全（structure-design.md）

在「标题公式（按契约）」后补：

```markdown
- 爽感约：冲突前置（「别忍了，这口气我替你出」/「那个甲方,你听好了」）
- 新奇约：反差前置（「用了十年的东西，设计初衷是 X」/「原来 X 还能这样用」）
```

## 🔴 P0-6 新奇约 P6 履约判定（contracts.md + SKILL.md P6）

```markdown
新奇约履约判定：完播率显著高于账号均值（≥ 均值 + 5pp）或评论出现「还能这样/学到了冷知识/长见识」；两者都无 = 未履约（猎奇未积累）。
```

---

## 🔴 P0-7 数据回流窗口修正（SKILL.md P6 + trust-engine.md 感知雷达）

平台核查结论：推荐周期为 7 天级，长尾流量真实存在，「48-72h 判定结束」不成立。

```markdown
P6 回流（两轮）：
- 首轮 48-72h：主流量高峰已过，采集 5 个数字 + 评论区，做履约归档与首轮归因（此时数据噪声最大，归因置信度默认「中低」）
- 终轮 第 7 天：复查长尾翻红（播放/涨粉是否二次爬升），补录终版数据，才写入版本库归档
长尾翻红记录：如第 4-7 天数据显著爬升，在版本库该条标注「长尾翻红」，归因时把「触发因素」单独记录（常与外部转发/搜索流量有关）
```

---

## 🟠 P1-1 契约判别表（contracts.md 反滥用红线前插入）

```markdown
## 契约判别表（低置信度回问用户）

| 契约 | 必须存在 | 互斥判据（有则不选此约） |
|------|---------|------------------------|
| 确定感约 | 可执行步骤/结论 | 无步骤只有故事 → 不是确定感 |
| 共鸣约 | 具体细节+情绪在场 | 有「冲突对象/敌人」→ 偏爽感；有「信息差」→ 偏优越 |
| 爽感约 | 冲突对象+彻底结局 | 有「教你方法」成分 → 转确定感 |
| 希望约 | 低起点+具体路径 | 路径不可复述 → 假希望，砍 |
| 优越约 | 信息差+「他们不会告诉你」姿态 | 有「认知反差但无信息差」→ 转新奇 |
| 新奇约 | 认知反差+解释 | 有「防坑/内幕」实用指向 → 转优越 |

判别流程：AI 按上表判主契约 → 若两契约都成立 → 标注双候选+置信度 → 问用户「这条你更想让观众____（学到方法/感到被理解/爽到/看到希望/觉得你懂/大开眼界）」→ 用户选的主契约写入 P4。
```

## 🟠 P1-2 节拍参数依据化（structure-design.md）

「每 15-20s 预期重建点」改为：

```markdown
**信息刷新点（弹性区间，约 8-20s）**：由内容密度决定，不是固定节拍。
依据：短视频前 5 秒的钩子-留存衔接有平台数据支撑；此后观众注意力以「信息增益」为单位计量——每段必须新增一个「新信息/新角度/新悬念」，间隔取决于该信息的复杂度，而非时钟。
检查法：逐段自问「这一段提供了什么上一段没有的信息？」答不出 = 该段该删或该提前收束。**对 3 分钟以上视频，禁止机械按 15-20s 切 15 个重建点**——重建点是信息单位不是时间单位。
```

## 🟠 P1-3 五元组观众任务来源（SKILL.md P2）

五元组组装后加一句：

```markdown
（观众任务在此阶段是**假设**，标注「待 P3 第三轮验证」；用户挑中后，P3 第三轮问「看完希望观众做什么」时校验并修正。）
```

## 🟠 P1-4 同构检验操作化（SKILL.md 全局铁律 9 的展开）

```markdown
同构检验模板：知识域 A × 知识域 B 的交叉，必须写出「B 的结构/机制，映射到 A 的一个具体场景」：
- 过：供应链(流通环节) × 砍价 = 「你买贵了，因为你不懂你付的钱里哪部分是流通成本」（结构映射：流通链路 → 价格构成）
- 砍：心理学 × 职场（写不出「心理学哪个机制 → 职场哪个具体行为」）= 标签拼接，砍
写不出 X→Y 的结构映射就砍——交叉不是两个名词并排，是一个机制落到一个场景。
```

## 🟠 P1-5 回流权重调整操作化（新增 koubo-assets/judge-weight.md + SKILL.md 宪法第四条落地）

```markdown
# 七官权重台账（P6 回流时更新）

规则：P6 判定「某官该杀没杀」（数据扑街但某官判过）记 1 次误判；误判 ≥3 次 → 该官判决降级（从「杀」降为「建议」）；
「某官冤枉好稿」（数据好但某官判杀，用户打回）也记 1 次，≥3 次 → 该官收敛阈值。
权重只影响 AI 的初审措辞，不影响仲裁优先级（红线事实官永远一票否决）。
```

## 🟠 P1-6 涨粉/看热闹操作化（SKILL.md P6 第 4 步）

```markdown
判定：涨粉数 ÷ 播放数 < 0.1%（示例阈值，可按赛道调）且播放显著高于均值 → 判「看热闹为主」，该条不写进 IP 资产，归因只提取结构因素不提取人设因素。
```

## 🟠 P1-7 冷启动口语官基准（six-judges.md 校准铁律补充）

```markdown
冷启动（无指纹、无转写稿）：口语官用「写→读→改」三步默认基准——先按常规写，用户朗读后以「念不顺的句子」为第一批评判样本；首条发布后指纹入档，第二条起切换为指纹校准。
```

## 🟠 P1-8 爆款公式承诺改写（SKILL.md 结果侧）

把「承诺第一百条的期望值是第一条的十倍」改为：

```markdown
唯一诚实的承诺：不承诺单条爆款。可检查的复利指标 = 版本库中 replicable 因素命中率逐条上升、归因置信度≥中的占比上升、高光库/指纹/对标库逐月增厚。
```

---

## 🟠 P1-9 行业对照补齐（来自行业框架核查子代理）

1. **JTBD 一问修正**（SKILL.md P0）：改为「你现在最想推进的一件事？——上次为这件事你实际做了什么、卡在哪？」（补过去时追问，才配得上 JTBD 标签；否则标题改「目标澄清」）。
2. **「说话指纹」改名**：为避免与语音学「声纹 voiceprint」冲突，全局改称「说话风格档案」或加注「文案语料指纹，非声纹」。
3. **对标库补三维度**（assets-templates.md 库五）：每条对标记录增加 ①数据归因（完播率/赞评比/流量来源结构/爆款率）②变现拆解（带货/知识付费/私域路径）③运营动作（更新频率/发布时间/评论区运营）。
4. **IP 定位卡补两项**：①变现锚定（这条 IP 最终靠什么赚钱，定位倒推）②定位验证动作（先发 10 条，第 10 条后回看数据收敛定位）。
5. **P6 归因分层**（SKILL.md P6 第 2 步）：完播问题→钩子/节奏归因；互动问题→观点/选题归因；涨粉问题→人设一致性归因。三层分开写，不混为一谈。
6. **爆款公式标注权重**：注明「结构/选题权重最高，时机/运气权重最低；教学用法，非计量模型」。

---

## 🟠 P1-10 心理学机制注释修正（structure-design.md 节拍参数表 + 结尾机制）

平台/心理学核查结论落地：
1. 「2s 钩子」措辞改「前 3 秒钩子生死线」：官方数据（巨量算数）流失从第 1-2 秒开始，行业主流口径为「黄金3秒」；「2s 死刑线」保留为内部阈值但注明「非官方指标，行业激进提法」。
2. 「2秒跳出率」数据采集改为「后台逐秒留存曲线 + 完播率 + 平均播放时长」口径（官方命名指标）。
3. 心理机制注释替换（防「心理学名词营销化」）：
   - 2s：~~信息缺口~~ → **注意定向/新颖性**（钩子=陌生信号拦拇指）
   - 5s：~~损失厌恶~~ → **open loop（未闭合回路）+ 预期后悔**（「不看完亏什么」）
   - 15-20s：~~认知失调~~ → **预期违背 + 惊讶 + 好奇**（「你以为的 X 是错的」）
   - 结尾：~~蔡格尼克效应~~ → 保留但弱化：「未完成感（need for closure）驱动的追更预期」，注明是通俗类比非严谨机制
4. 全文档禁用「8 秒注意力」类伪科学数据（本 skill 未使用，补丁时确认不引入）。

---

## 🟡 P2 增强（新增参考文件或章节）

1. **发布执行清单**（新文件 references/publish-runbook.md）：发布时间窗（赛道流量高峰）、封面制作三选一执行、标题 A/B 测试、发布后 1 小时评论区引导话术。
2. **评论区运营 SOP**：置顶引导评论（让一级信号更密）、回复节奏（前 48h 回复所有带情绪评论）、「契约密码」实时归档。
3. **变现路径**：主契约→变现匹配表（确定感约→课程/咨询；希望约→社群陪跑；爽感约→带货引流慎用），一条视频最多一个营销动作与变现衔接。
4. **合规红线**（并入事实官）：医疗/金融/功效断言/夸大宣传的自查表 + 「数据、案例可查证」之外加「宣称可被证伪」。
5. **心理安全**：自曝素材强度分级（1-3 级），用户可设「自曝上限」，宪法新增一条「不逼问用户不愿讲的事」。
6. **指纹漂移更新**：季度复核时重采 10 分钟对话，比对旧指纹，漂移大则更新并注明变化原因。
7. **选题池 backlog**：P2 未选中候选入池，标注有效期（热点类 7 天、常青类 90 天），过期清理。